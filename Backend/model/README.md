# Model Weights

Place production model files in this folder before running backend inference.

Expected examples:

- `best_convnext_skin_cancer.pth`
- `chimaera_v2_final.h5`
- `best.pt` or `best.pt.zip` (YOLO)

For large model artifacts, use Git LFS or private storage and update environment/model paths accordingly.

